function [a, eta] = generate_traj(limits, par)
    Clmax = limits(1); Vmax = limits(2); nu_min = limits(3); nu_max = limits(4);
    Tmin = limits(6); Tmax = limits(5); hmin = limits(7);
    
    % no. of harmonics
    N = par(1);
    
    
end